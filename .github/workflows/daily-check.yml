name: Daily price check

# The daily schedule is deliberately disabled. Every scheduled run for nine
# days returned HTTP 403 from every shop: retailers fingerprint the TLS
# handshake, and GitHub's runners are in datacenter IP ranges they block. The
# real schedule now runs on the TrueNAS server (see deploy/README.md), and two
# schedulers writing data/ would only fight each other.
#
# Manual runs still work from the Actions tab if you want to test something.
on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Check prices without recording or emailing"
        type: boolean
        default: false

# Never let two runs write history at the same time.
concurrency:
  group: price-check
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Check prices and events
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          ALERT_EMAIL_TO: ${{ vars.ALERT_EMAIL_TO }}
        run: |
          uv run pricetracker daily ${{ inputs.dry_run && '--dry-run' || '' }}

      - name: Commit updated price and event history
        if: ${{ !cancelled() && !inputs.dry_run }}
        run: |
          if [ ! -d data ]; then
            echo "No data directory — the check did not get far enough to record anything."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/   # data/debug/ is git-ignored
          if git diff --staged --quiet; then
            echo "No new data to commit."
            exit 0
          fi
          git commit -m "chore: record daily check $(date -u +%Y-%m-%d) [skip ci]"
          # Another run may have pushed while we were fetching pages.
          for attempt in 1 2 3; do
            if git push; then exit 0; fi
            echo "Push failed, rebasing and retrying ($attempt/3)..."
            git pull --rebase --autostash
            sleep $((attempt * 5))
          done
          echo "Could not push history after 3 attempts." >&2
          exit 1

      - name: Upload pages that could not be parsed
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v4
        with:
          name: unparsed-pages
          path: data/debug/
          if-no-files-found: ignore
          retention-days: 7
